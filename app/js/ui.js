// ui.js — desenha as telas (camada de apresentacao).
// A UI nunca fala direto com o IndexedDB — so via dados.js.
// Telas: Inicio, Treino livre, o card (3 etapas), Progresso (mapa do cerebro).
// TODO(sonnet).
