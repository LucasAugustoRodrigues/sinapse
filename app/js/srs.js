// srs.js — motor de repeticao espacada (FUNCOES PURAS).
// Recebe estado + `hoje`, devolve novo estado. Sem Date.now() interno, sem IO.
// Especificacao completa em PLANEJAMENTO.md §7 (SM-2 adaptado; nota = acerto x confianca;
// notaFinal = pior(notaRec, notaRes); graduacao/crescimento/lapso; fila do dia).
// TODO(sonnet): derivarNota, revisar, construirFila, forcaHabilidade, dominio.
