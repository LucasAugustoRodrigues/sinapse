// dados.js — CAMADA DE DADOS (padrao Repository).
// A UNICA peca que fala com o armazenamento.
// v1: le questoes.json + grava progresso no IndexedDB.
// v2: troca-se SO este arquivo por chamadas a API; o resto do app nao muda.
// TODO(sonnet): carregarQuestoes(), lerProgresso(cardId), salvarProgresso(card), ...
