{
  "name": "sinapse-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/copiar-dados.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copiar-dados.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "fake-indexeddb": "^4.0.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
